(function() {
  const errorLabel = $("#new_comment_for_Comment_<%= @form.commentable.id %> .form-error")
  errorLabel.html("<%= @form.errors.messages.values.join(", ") %>");
  errorLabel.addClass("is-visible");
})();
