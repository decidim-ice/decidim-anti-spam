#!/usr/bin/env bash
# Run RuboCop, ERB Lint, and RSpec (same as CI expectations).
set -euo pipefail
cd "$(dirname "$0")/.."
unset DATABASE_URL
export RAILS_ENV=test
bundle exec rubocop .
bundle exec erblint --lint-all --enable-all-linters
bundle exec rspec "$@"
